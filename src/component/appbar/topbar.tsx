import { AppBar, Toolbar } from "@mui/material";

const TopBar = () => {
    return(
        <AppBar>
            <Toolbar>
                
            </Toolbar>
        </AppBar>
    )

}
export default TopBar;